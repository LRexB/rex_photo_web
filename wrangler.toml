# Cloudflare Pages configuration
name = 'rexphotoweb'
compatibility_date = "2024-03-01"
pages_build_output_dir = "dist"

# Build configuration
[build]
command = "npm run build"
cwd = "./"

# Environment aliases
[env.production]
route = "rexbenning.com"
zone_name = "rexbenning.com"
