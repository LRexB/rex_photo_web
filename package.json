{
  "name": "rex-photo-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate:galleries": "node scripts/generate-gallery-manifest.mjs",
    "dev": "npm run generate:galleries && vite",
    "build": "vite build",
    "prebuild": "npm run generate:galleries",
    "preview": "vite preview",
    "lint": "eslint . --ext .js,.jsx"
  },
  "dependencies": {
    "exifr": "^7.1.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^7.0.0"
  }
}
